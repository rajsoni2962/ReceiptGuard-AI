# Frontend API URL Configuration
# In production on Vercel, leave empty or unset so the frontend communicates directly via relative /api rewrites.
# In local development, defaults automatically to http://localhost:8000 when unset.
VITE_API_URL=
